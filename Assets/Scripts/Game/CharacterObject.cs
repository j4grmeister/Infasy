using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CharacterObject : MonoBehaviour {
	/*
	public bool isPlayer;

	Character character;

	// Use this for initialization
	void Start () {
		if (isPlayer) {
			Game.current.playerCharacter.characterObject = this;
			character = Game.current.playerCharacter;
		}
	}
	
	// Update is called once per frame
	void Update () {
		gameObject.transform.position = character.position * Tile.tileSize / 100;
	}

	public void SetCharacter(Character c) {
		character = c;
	}
	*/
}
